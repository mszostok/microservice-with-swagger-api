# Simple micro-service with swagger spec
Sample to present how to generate swagger spec from source code by swagger-maven-plugin

## Usage

You can quickly generates swagger.json and static document by
```bash
mvn compile
````
